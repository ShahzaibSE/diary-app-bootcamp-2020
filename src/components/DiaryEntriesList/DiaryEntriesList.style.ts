import { makeStyles, useTheme, Theme, createStyles } from '@material-ui/core/styles';

export const diaryEntriesListStyles = makeStyles((theme: Theme) => 
        createStyles({
            root: {
                "& > *" : {
                    
                }
            }
        })
)